<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pjm.userservice.mapper.UserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.pjm.userservice.entity.User">
        <id column="id" property="id" />
        <result column="user_account" property="userAccount" />
        <result column="user_password" property="userPassword" />
        <result column="user_name" property="userName" />
        <result column="user_tel" property="userTel" />
        <result column="user_position" property="userPosition" />
        <result column="user_employ" property="userEmploy" />
        <result column="user_address" property="userAddress" />
        <result column="user_header" property="userHeader" />
        <result column="enabled" property="enabled" />
        <result column="version" property="version" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_account, user_password, user_name, user_tel, user_position, user_employ, user_address, user_header, enabled, version
    </sql>

</mapper>
